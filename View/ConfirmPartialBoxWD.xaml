<Window x:Class="SX3_SCANER.View.ConfirmPartialBoxWD"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Xác nhận đóng thùng lẻ"
        Width="720"
        SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        FontFamily="Segoe UI">

    <Border Margin="18" Background="White" BorderBrush="#F59E0B" BorderThickness="1.5" CornerRadius="24">
        <Border.Effect>
            <DropShadowEffect Color="#334155" BlurRadius="30" ShadowDepth="0" Opacity="0.25"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#FFFBEB" CornerRadius="24,24,0,0" Padding="24,20" BorderBrush="#FDE68A" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="70"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <Border Width="56" Height="56" CornerRadius="28" Background="#F59E0B">
                        <TextBlock Text="!" FontSize="34" FontWeight="Black" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="XÁC NHẬN ĐÓNG THÙNG LẺ" FontSize="27" FontWeight="Black" Foreground="#92400E"/>
                        <TextBlock Text="Thùng chưa đủ số lượng tiêu chuẩn. Cần xác nhận trước khi đóng." FontSize="14" FontWeight="SemiBold" Foreground="#92400E" TextWrapping="Wrap" Margin="0,4,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Margin="24,22,24,18">
                <Border CornerRadius="14" Background="#FFFBEB" BorderBrush="#FDE68A" BorderThickness="1" Padding="14,12" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="TRẠNG THÁI" FontSize="13" FontWeight="Black" Foreground="#B45309"/>
                        <TextBlock Text="Chưa đủ số lượng tiêu chuẩn" FontSize="19" FontWeight="Black" Foreground="#92400E" TextWrapping="Wrap" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
                <Border CornerRadius="14" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="14,12" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="SỐ LƯỢNG HIỆN TẠI" FontSize="13" FontWeight="Black" Foreground="#64748B"/>
                        <TextBlock x:Name="txtQuantity" FontSize="34" FontWeight="Black" Foreground="#C2410C" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
                <Border CornerRadius="14" Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" Padding="14,12">
                    <StackPanel>
                        <TextBlock Text="KHI NÀO NÊN ĐÓNG LẺ" FontSize="13" FontWeight="Black" Foreground="#1D4ED8"/>
                        <TextBlock Text="Chỉ xác nhận khi thiếu hàng thực tế, kết thúc lô hoặc được quản lý cho phép." FontSize="17" FontWeight="Bold" Foreground="#0F172A" TextWrapping="Wrap" LineHeight="25" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <Border Grid.Row="2" Background="#F8FAFC" CornerRadius="0,0,24,24" Padding="24,16" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="14"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="XÁC NHẬN ĐÓNG LẺ" Height="50" FontSize="16" FontWeight="Black" Foreground="White" Background="#F59E0B" BorderThickness="0" IsDefault="True" Click="BtnConfirm_Click">
                        <Button.Template><ControlTemplate TargetType="Button"><Border CornerRadius="14" Background="{TemplateBinding Background}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border></ControlTemplate></Button.Template>
                    </Button>
                    <Button Grid.Column="2" Content="QUAY LẠI SCAN" Height="50" FontSize="16" FontWeight="Black" Foreground="#0F172A" Background="White" BorderBrush="#CBD5E1" BorderThickness="1" Click="BtnCancel_Click">
                        <Button.Template><ControlTemplate TargetType="Button"><Border CornerRadius="14" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border></ControlTemplate></Button.Template>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
